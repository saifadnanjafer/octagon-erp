task: T2.3
lane: A
agent: claude-opus-4-8
started: 2026-07-13T11:25:00Z
status: DONE
gates: |
  A pass (317 JS files). F pass (live restarted, app.js v23, fresh boot ZERO
  console errors, employees=26, drawer opens on a real move). C not regressed
  (T2.3 adds no routes/pages/templates — utility scripts + a DOM-injected
  drawer; route_health builder returns the same 36-route set; the full
  route_health page flow is heavy under headless automation but throws no
  console errors). G pass. Full functional verification on the isolated 8090
  copy (finance moves are real ledger mutations — never live):
  - create/update/post/cancel a move -> 4 history entries in correct order
    (created, diff(origin {from:'',to:'tc-updated-origin'}), posted, cancelled).
  - FROZEN-ZONE guard: record('employees',...) and record('attendance_records',
    ...) both return null and add nothing.
  - Ring buffer: 5050 records -> log length capped at exactly 5000, oldest
    evicted, newest retained.
  - Drawer opens + renders entries.
  - PERSISTENCE: a created-event survived PentagonDB.load({force:true}) (read
    back via the refreshed cache) — proves it persisted and reads consistently.
notes: |
  New modules/change-tracker.js — TrackChanges.record / recordInto(db,...) /
  recordDiff / list / openDrawer / closeDrawer. Entries -> omni.changeLog ring
  buffer (cap 5000, oldest evicted). «سجل التغييرات» drawer (self-injected
  styles + overlay) reachable from record modals.

  KEY ARCHITECTURE FINDING (drove the design): the bare `omni` global and
  PentagonDB.getCached().omni START as the same object at boot but DIVERGE
  after any PentagonDB.load({force:true}) (the finance tab does this on every
  render). The authoritative, persisted store is PentagonDB.getCached().omni
  (finance mutate writes land there; saveData() POSTs getCached(); force-loads
  refresh it from the server). So TrackChanges.O() resolves via
  PentagonDB.getCached().omni FIRST (bare omni only as fallback). Inside a
  PentagonDB.mutate the passed db IS getCached(), so recordInto(db,...) and
  record()/list() all target the SAME object — write, read, and persistence
  stay consistent. (First attempt wrote via bare omni and read 0 back — fixed.)

  WIRING (add-only):
  - services/financeService.js: recordInto(db, 'account_moves', ...) inside the
    create/update/post/cancel mutators (persisted atomically with the move).
    updateMove snapshots a pre-image and records the field-level diff.
  - app.js renderAccountMoveDetail: «📝 السجل» button -> TrackChanges.openDrawer.
  - modules/work-orders.js woTransition(): record('jobOrders', wo.id, {state:
    {from,to}}) (persisted by the existing save()). NOTE: omni.jobOrders is
    empty on live (see the T1.1/§13 flag) so this path is code-correct but not
    live-verifiable; treated as the jobOrders adopter per the plan.

  FROZEN ZONE (§1): employees/attendance/timesheet/payroll/salary edits are
  never tracked (isFrozen guard) — their existing audit stays as-is.

  Client-only change (no server.js edit); app.js->v23, change-tracker->v2,
  financeService.js + work-orders.js ?v= bumped so the live site refetches them.
  Admin-CRUD omni-layer adoption left opportunistic (record() API is ready).
