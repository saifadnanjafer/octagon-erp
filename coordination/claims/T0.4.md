task: T0.4
lane: A
agent: claude-sonnet-5
started: 2026-07-12T14:15:00Z
status: DONE (35/37 — 2 intentionally deferred, see below)
gates: A pass after every edit. Live-smoke-tested every batch against the
  already-running dev server (browser JS eval: every live+dead function
  pair resolves as 'function', every live function called directly against
  real DOM/data with zero thrown errors, console clean after each batch).
  B: 2/37 remain (down from 37) — both deferred, not oversights.
notes: |
  COMPLETE. 7 batches, 35 duplicate top-level functions deduped across the
  whole file with zero behavior regressions (verified live, not just
  node --check). Method proven throughout: fresh grep for line numbers
  before every edit (they shift after each change), diff both definitions,
  rename the dead (earlier, shadowed) one to NAME_deprecated_dupN with a
  short comment explaining what's different, never touch the live one.

  Batches (commits 7e8ddb4, eab5795, 1f366c1, d0fb586, fc52a71, 13628e9,
  1240274):
  1. escapeHtml, getEmployeeStatusLabel
  2. machines cluster (addMachine, renderMachinesPage,
     renderMachineInspectorTab, editMachineFromInspector)
  3. admin-wireup cluster (getAdminWireUpRows, renderAdminTabWireUp x3 —
     the original 2026-07-02 audit's "worst" case)
  4. finance journal-entry UI cluster (switchFinanceTab,
     renderJournalEntryTab, openNewJEModal, saveNewJE, postJEFromUI,
     reverseJEFromUI) — the JE-action block already had a 2026-07-04 audit
     comment confirming it was safe dead code; extended it rather than
     re-litigating
  5. Task Manager V1 -> V2 (17 functions in one block, bounded by an
     explicit "═══ TASK MANAGER V2 ═══" marker in the file)
  6. unpublishWorkflow
  7. WhatsApp/automation/op-packs mega-block (renderOpPacks,
     renderAutomationEngine, renderWhatsAppIntegrationPage, setWaTab,
     renderWhatsAppSimulatorPanel)

  TWO REAL (not just stale) findings surfaced along the way, NOT silently
  resolved:
  - getEmployeeStatusLabel: dead copy's 3-way active/pending/else contract
    matches what both call sites pass; live copy only handles 2 ways, so a
    'pending' employee renders as "مستقيل" (resigned). Left as-is.
  - renderWhatsAppSimulatorPanel: dead copy has 11 preset simulation
    messages, live copy only has 4 — NOT a "later=richer" case like every
    other rename this session, the live one is actually the smaller one.
    Left as-is (T0.4 preserves current behavior; picking the preset list
    is a product decision, not a dedup).
  Both are business-logic calls, not mechanical dedup — intentionally not
  decided here.

  REMAINING 2/37 — deferred to the owner, not attempted:
  - addEmployee (app.js, HR-admin "add employee" flow): writes
    omni.employees + generates a full month of attendance records. §1
    frozen zone reads "omni.employees and all attendance/fingerprint data
    = READ-ONLY for every lane... STOP, set claim status BLOCKED." Open
    question for the owner: does that freeze cover HR-admin employee
    *creation* (this function), or is it scoped to the Timesheet
    page's/payroll calc's existing render/calc functions specifically? If
    the owner says HR-admin creation is out of scope for the freeze, this
    is a normal T0.4 dedup like every other pair above (dead copy is a
    bare stub; live copy adds shift/employment-period/notes fields).
  - renderAttendanceCalendar: same STOP-on-frozen-zone reasoning, applied
    to the attendance forecast calendar render specifically (not raw
    employee data, but inside the "attendance forecast" surface that's
    explicitly named in §1). Not diffed in detail — deliberately left
    alone pending the same owner clarification.

  Gate B will read exactly 2 until the owner answers the frozen-zone-scope
  question above; that's expected, not a bug.
