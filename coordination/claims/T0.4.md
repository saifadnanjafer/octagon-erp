task: T0.4
lane: A
agent: claude-sonnet-5
started: 2026-07-12T14:15:00Z
status: HANDOFF
gates: A pass after every edit (node --check app.js). Live-smoke-tested
  batch 2 against the already-running dev server (browser JS eval: all
  renamed + live function pairs resolve, no thrown errors, console clean).
  B still fails (31 names remain) — expected, this is a multi-session task.
notes: |
  Re-ran Gate B fresh (not the stale 2026-07-02 list): 37 duplicate
  top-level function names, not 23 — grown since the original T0.4 scoping,
  independently confirmed by the concurrent LANE-V codex verifier's Gate B
  fail earlier this session. Most are NOT random accidental dupes: they
  cluster into whole blocks that look like abandoned Phase-4 extraction
  attempts where the OLD in-app.js block was never deleted after a rewrite
  (matches Phase 4's own priority list almost exactly: WhatsApp, task
  manager, machines, admin-wireup, automation-engine/op-packs).

  METHOD (proven safe over 2 batches, use this for all remaining work):
  1. Fresh `grep -noE "^(async )?function NAME\b" app.js` for exact current
     line numbers — THEY SHIFT after every edit, never reuse stale numbers.
  2. Read both full definitions. Diff them. Default finding across every
     pair checked so far: the LATER definition is a more complete rewrite
     (richer UI/logic) and the EARLIER one is an inert, fully-shadowed stub
     — safe to rename with zero behavior change since JS "last declaration
     wins" means the earlier one never executes today regardless.
  3. EXCEPTION FOUND: getEmployeeStatusLabel's dead copy has a 3-way
     active/pending/else contract matching what both call sites actually
     pass, while the live 2-way copy mislabels 'pending' employees as
     resigned. This is a real behavior question, not just staleness — left
     both as-is (renamed dead, flagged in-code) rather than deciding which
     is "correct" myself.
  4. Rename the dead one to `NAME_deprecated_dupN` with a 1-3 line comment
     explaining what's different / why it's dead. NEVER touch the live one.
     Add-only — never delete.
  5. node --check app.js after every edit.
  6. Batch commit (~2-5 functions or one cohesive cluster), smoke-test via
     the live dev server (already running on :8080 for T0.2 — reuse it,
     don't spin up a second one) using browser JS eval: check
     `typeof NAME` / `typeof NAME_deprecated_dupN` are both 'function',
     call the live one directly where safe, check console for errors.
     (Note: calling `switchPage('x')` via raw JS eval outside a real click
     event did NOT reliably activate the target page in this SPA — verify
     via direct function calls / typeof checks instead, not page nav, or
     debug switchPage's activation guard first if page-level rendering
     verification is required.)

  DONE this session (batch 1+2, commits 7e8ddb4, eab5795):
  - escapeHtml (verified byte-identical dead copy, zero risk)
  - getEmployeeStatusLabel (dead copy renamed, semantic mismatch flagged
    in-code, NOT resolved — business-logic call for the owner)
  - addMachine, renderMachinesPage, renderMachineInspectorTab,
    editMachineFromInspector (machines cluster, fully deduped + smoke-tested)

  EXPLICITLY DEFERRED — frozen zone (§1 STOP instruction, not merely
  "safe by analogy"): addEmployee (11053/29463ish) writes omni.employees +
  generates a full month of attendance records. Do NOT dedupe without
  owner clarification on whether HR-admin employee creation counts as
  frozen "omni.employees...data" or is scoped narrower to
  Timesheet-page/payroll-calc functions specifically.

  REMAINING (31 names, fresh line numbers as of this commit — re-grep
  before touching, they will have shifted again):
  - TASK MANAGER CLUSTER (~17 names, lines ~19100-20350, looks like the
    ENTIRE task-manager section duplicated as one block): unpublishWorkflow
    19114/19530, toggleTaskManagerView 19560/19998, renderTaskManager
    19562/20063, addTaskSpace 19629/20197, addTaskDepartment 19630/20198,
    addTaskSection 19631/20199, addTaskType 19632/20200, addClickupTask
    19633/20201, editClickupTask 19634/20210, renderTaskInspectorTab
    19636/20212, updateTaskField 19723/20337, deleteClickupTask 19733/20342,
    renameTaskManagerLevel 19777/20343, addSubtask 19805/20344,
    toggleSubtask 19806/20345, deleteSubtask 19756/20346,
    updateSubtaskTitle 19767/20347.
  - FINANCE JOURNAL-ENTRY UI CLUSTER (6 names, lines ~5400-5600 vs
    ~29600-31250): switchFinanceTab 5415/29598, renderJournalEntryTab
    5432/29638, openNewJEModal 5511/31088, saveNewJE 5553/31123,
    postJEFromUI 5577/31147, reverseJEFromUI 5587/31246.
  - ADMIN WIRE-UP CLUSTER (2 names, renderAdminTabWireUp is a TRIPLE at
    23957/24037/24242 — the original T0.4 spec called this "worst"):
    getAdminWireUpRows 23940/24005, renderAdminTabWireUp (×3).
  - WHATSAPP + AUTOMATION-ENGINE + OP-PACKS MEGA-BLOCK (5 names spanning a
    huge non-adjacent range, ~750+ lines, near the end of the file —
    biggest remaining cluster, needs the most care): renderWhatsAppSimulatorPanel
    32726/34633, renderWhatsAppIntegrationPage 32770/40037, setWaTab
    32940/40142, renderOpPacks 20893/39800, renderAutomationEngine
    31680/40517.
  - STANDALONE: renderAttendanceCalendar (frozen-zone ADJACENT — attendance
    calendar render, not raw omni.employees data, but still inside the
    "attendance forecast" surface; diff carefully and consider treating
    like addEmployee — flag rather than auto-resolve — before touching).

  Suggest continuing in the same order (biggest-cohesive-wins-first per
  Phase 4's own priority list): admin-wireup (smallest, 2 names) ->
  finance-JE -> task-manager -> whatsapp/automation/oppacks mega-block ->
  decide renderAttendanceCalendar with the owner.
