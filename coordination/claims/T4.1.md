task: T4.1
lane: A
agent: claude-opus-4-8
started: 2026-07-13T11:55:00Z
status: DONE
gates: |
  A pass (319 JS files incl. the new module; node --check app.js + module both
  clean). B pass — NO new duplicate function names introduced (the only two
  dups codebase-wide remain the pre-existing addEmployee / renderAttendanceCalendar
  from T0.4, untouched). F pass (live reload, fresh-tab boot ZERO console
  errors, employees=26). Render smoke: renderWhatsAppIntegrationPage populates
  pageWhatsapp with ~2478 chars of correct Arabic UI — identical behaviour to
  control pages (finance 2194, calculator) under the same headless test. All 40
  moved fns are window globals and execute (classifyWhatsAppLine ->
  finance_request 88%, simulateInboundWhatsApp runs using the MOVED
  WHATSAPP_SIMULATION_PRESETS const with no ReferenceError, setWaTab runs).
  C/D: route-health shows every one of the 36 routes with the identical
  {renderOk:true, navOk:true, pageOk:false} shape (pageOk is false for ALL
  routes in a fresh un-navigated headless session — pages hydrate on visit);
  whatsapp is byte-for-byte the same shape as the calculator control, so its
  renderOk+navOk are intact. Workshop stabilization 11/12, the one failure
  being the "system-health-fully-green" aggregate that reads that same
  un-hydrated route health — a pre-existing headless artifact, NOT introduced
  by this extraction (whatsapp renderOk/navOk both true).
notes: |
  Phase 4 batch #1 COMPLETE. Extracted the WhatsApp integration cluster VERBATIM
  from app.js into modules/whatsapp-integration.js (move != improve — a Node
  script with hard boundary assertions did the cut; it aborted once on a wrong
  end line and wrote nothing until the boundary was corrected, so app.js was
  never left half-edited).

  Moved 40 functions + WHATSAPP_SIMULATION_PRESETS, in 3 original contiguous
  app.js blocks concatenated in file order:
   - A 32572-33040 (group mgr, analytics, image ingest, 3 *_deprecated_dup1,
     filterWaGroup)
   - B 34083-34749 (WHATSAPP_SIMULATION_PRESETS + classify/match/suggestions/
     simulator)
   - C 40064-40306 (Meta API config, renderWhatsAppIntegrationPage, setWaTab)
  app.js 40921 -> 39541 lines (-1379). module = 1394 lines. addSalesLeadFromWhatsApp
  intentionally LEFT in app.js (it belongs to the Sales cluster, a later batch).

  Kept as classic-script top-level functions (no IIFE) so they remain window
  globals exactly as before; module loaded AFTER app.js (before omni-language-fix.js)
  so runtime references to app.js helpers resolve. omni-language-fix.js only had
  WhatsApp TRANSLATION STRINGS (no function monkey-patch) so nothing was pinned.
  app.js ?v= -> v24. Client-only; no server change.
