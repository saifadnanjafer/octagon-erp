task: T0.2
lane: A
agent: claude-sonnet-5
started: 2026-07-12T13:00:00Z
status: DONE
gates: F pass (server boots clean, /api/db responds, employees=26 intact, finance present, no errors in server log)
notes: Deviated from the plan's literal "restart via start.ps1" — verified
  start.ps1 and start-all.ps1 are both stale/legacy (start.ps1 is a
  pre-SQLite plain-PowerShell static file server with no DB/WAL involvement
  at all; start-all.ps1 launches the dead erp-local/ experiment). The real
  launcher, per README.md and docs/RELEASE_CANDIDATE_PILOT_CHECKLIST.md, is
  `node server.js` (port 8080). No server was running beforehand (checked
  netstat + process list with command lines — zero listeners on 8080/3000,
  the 5 running node.exe processes were Adobe CC background services + MCP
  servers, unrelated). Started `node server.js` in the background; confirmed
  via `node:sqlite` (DatabaseSync, matching what server.js actually uses —
  NOT better-sqlite3 as the plan's example snippet assumed) that
  `PRAGMA journal_mode` now reports `wal`. Server left running (the app is
  otherwise unusable) — PID captured in this session's background job.
  Suggest updating AGENT_EXECUTION_PLAN.md T0.2's launch instructions from
  start.ps1 to `node server.js` for future agents (not done here — LANE-A
  content edit, will fold into the next §13/plan touch-up).
