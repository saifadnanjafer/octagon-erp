task: T1.3
lane: A
agent: claude-sonnet-5
started: 2026-07-12T16:00:00Z
status: DONE
gates: A pass (85 JS files). F pass (live server restarted with the new
  code, clean boot, zero console errors, employees=26). E pass (attendance
  canary 6/6 after restart). G pass. Manual curl verify per §3.8 done
  against an isolated 8090/8091 copy server (never live) — see notes.
notes: |
  server.js POST /api/db now has two independent, tenant-agnostic guard
  layers: (1) X-Octagon-Full-Sync: yes header required on every request,
  checked before parsing the body; (2) HARD_PROTECTED_COLLECTIONS —
  employees, account_moves, finance.customers, finance.transactions,
  finance.accounts, omni.jobOrders — rejected (409, names the collection,
  logged to server-write-guard.log) if a payload would replace a
  currently non-empty one with empty/missing. Client callers updated:
  saveData() (app.js) and PentagonDB.save() (services/auditService.js).

  KEY FINDING that shaped the design: the existing
  SERVER_TENANT_COLLECTIONS protection (which already covers most of
  these paths) is entirely gated behind getOrgSettings().multiTenant —
  for this single-tenant workshop deployment that's OFF, so
  applyServerTenantProtectionToDatabase() is a complete no-op today.
  account_moves/finance.*/omni.jobOrders had ZERO real protection in
  production before this task. Also: the existing employees-specific
  check only catches "present but empty array" (Array.isArray(undefined)
  is false), not "key entirely absent" — left that logic untouched
  (proven, working) and let the new tenant-independent check catch the
  gap instead of modifying it.

  VERIFICATION (isolated copy, never the live DB):
  - Copied database.json + database.db(+wal/shm) + .env to a scratchpad
    dir, ran `OCTAGON_DB_FILE=... OCTAGON_SQLITE_DB_FILE=... PORT=8090
    node server.js` against the copy only.
  - Port 8090 was concurrently occupied by another agent's own verify
    pass at the exact same moment (confirmed via process command line) —
    my instance auto-fell-back to 8091 exactly as designed, zero
    conflict, zero manual intervention needed. Good real-world proof the
    fallback-port behavior works under real concurrent multi-agent load.
  - Test 1 (naive probe, no header): 409, copy's data completely
    untouched (employees=26, account_moves=568, finance.customers=1,
    finance.transactions=526 all unchanged) — matches the plan's exact
    manual-verify instruction.
  - Test 2 (header present, employees key entirely absent + account_moves
    emptied): 409 naming "employees" first — confirms the new check
    catches the gap the old logic missed.
  - Test 3 (header present, valid employees + account_moves emptied): 409
    naming "account_moves" specifically — confirms the loop checks every
    protected collection in order, not just stopping at the first pass.
  - Test 4 (header present, fully valid full-sync payload): 200, persisted
    correctly.
  - verify.lock (stale from a prior pass, 61 min old, no active listener
    confirmed via netstat) taken over per protocol, removed after; scratch
    dir removed; verify server process stopped.
  - Live server (:8080) then stopped and restarted with the new code
    (this itself is a normal part of deploying the change, same as T0.2);
    confirmed clean boot, 0 console errors, attendance canary 6/6, a real
    saveData() call from the live app sent the header and returned 200,
    employees still 26.
