# Page Disposition Report — Product Recovery 1

Generated by `scripts/product/build-disposition-report.mjs` — do not hand-edit.
Before: docs/product/PAGE_FUNCTIONAL_LEDGER.md as generated 2026-08-09T07:00:12.405Z (session start).
After: docs/product/PAGE_FUNCTIONAL_LEDGER.json as generated 2026-08-21T09:26:53.593Z (current).

## Primary page count

| | Before | After |
|---|---|---|
| Primary workspaces | 231 | 231 |
| Moved to tab this pass | — | 0 (5 prior consolidations already in place before this session; see docs/navigation/OWNER_CONSOLIDATION_DECISIONS.md; 2 more A-class and 10 B-class candidates await owner sign-off — see OWNER_PRODUCT_DECISIONS.md) |
| Moved to dialog/action this pass | — | 0 |
| Hidden/internal this pass | — | 0 |
| Retired this pass | — | 0 |
| **Capabilities lost** | — | **0** |

No navigation consolidation was executed in this pass. Every candidate found
(PAGE_OVERLAP_MATRIX.md, OWNER_PRODUCT_DECISIONS.md) needs an owner call on intent
before a page id changes meaning for whoever already has it bookmarked — see
OWNER_PRODUCT_DECISIONS.md. This pass focused on making the existing 231 honest:
classifying every one with evidence and fixing what runtime evidence proved broken.

## Functional state

| State | Before | After | Δ |
|---|---|---|---|
| STRONG | 80 | 80 | 0 |
| USABLE | 113 | 113 | 0 |
| THIN | 31 | 38 | +7 |
| BROKEN | 7 | 0 | -7 |
| Dead ends (content, no action, no link) | 2 | 16 | +14 |

## Fixes landed this pass

| Fix | Root cause | Pages affected |
|---|---|---|
| `pick_task_queue` crash guard (modules/build09-pick-task-queue-workspace.js) | `allRows` used before first assignment when `onActivate` threw on a permission denial — the error panel rendered behind a page that had already crashed | 1 (`pick_task_queue`) |
| `build12` overview `scoped()` tenantId fix (platform/build12/index.mjs) | Every `scoped()` call in the combined overview resource omitted `tenantId`, shifting each positional arg left by one — the order string landed in the tenantId slot and the limit number in the order slot, producing an invalid `ORDER BY <number>` SQLite throws on | 3 (`ai_overview`, `marketing_overview`, `people_development_overview`) |
| `build12` per-resource order-column fix (platform/build12/index.mjs) | The generic query-map path defaulted every resource to `ORDER BY created_at DESC`; `people_competencies` only has `updated_at` and `build12_event_registrations` only has `registered_at` — both threw a 500 on every request | 3 (`competency_profiles`, `event_checkin`, `event_registrations`) |
| WMS/shopfloor/quality `:view` permission registration (database/migrations/090, scripts/review/roles.mjs) | 16 of the 20 permission tokens `platform/api/build09.mjs` `BUILD09_RESOURCE_PERMISSIONS` requires for `GET /api/v1/wms/*` were never registered in `authorization_permissions` at all — `registry.assertKnown()` fails closed on an unregistered token regardless of what any role is granted, so no role (review or production) could ever pass these checks | 18 pages — see PAGE_RUNTIME_ERROR_LEDGER.md for the full list |

## Emptiness cause distribution (current)

| Cause | Pages |
|---|---|
| H | 31 |
| G | 7 |

## Recommended disposition (current)

| Disposition | Pages |
|---|---|
| KEEP_PRIMARY | 193 |
| DEFER | 31 |
| STRENGTHEN | 7 |

## Pages still needing attention (38)

| Page | Priority | Cause | Disposition |
|---|---|---|---|
| `dock_checkin` | P0 | H — page has no meaningful domain implementation behind it | DEFER — no implementation to seed a fixture against yet |
| `dock_schedule` | P0 | H — page has no meaningful domain implementation behind it | DEFER — no implementation to seed a fixture against yet |
| `expiration_queue` | P0 | H — page has no meaningful domain implementation behind it | DEFER — no implementation to seed a fixture against yet |
| `production_issue_return` | P0 | H — page has no meaningful domain implementation behind it | DEFER — no implementation to seed a fixture against yet |
| `production_material_requests` | P0 | H — page has no meaningful domain implementation behind it | DEFER — no implementation to seed a fixture against yet |
| `production_receipt` | P0 | H — page has no meaningful domain implementation behind it | DEFER — no implementation to seed a fixture against yet |
| `quality_hold_queue` | P0 | H — page has no meaningful domain implementation behind it | DEFER — no implementation to seed a fixture against yet |
| `recall_analysis` | P0 | H — page has no meaningful domain implementation behind it | DEFER — no implementation to seed a fixture against yet |
| `rework_workspace` | P0 | H — page has no meaningful domain implementation behind it | DEFER — no implementation to seed a fixture against yet |
| `scrap_approval` | P0 | H — page has no meaningful domain implementation behind it | DEFER — no implementation to seed a fixture against yet |
| `shopfloor_terminal` | P0 | H — page has no meaningful domain implementation behind it | DEFER — no implementation to seed a fixture against yet |
| `wave_execution` | P0 | H — page has no meaningful domain implementation behind it | DEFER — no implementation to seed a fixture against yet |
| `work_orders` | P0 | H — page has no meaningful domain implementation behind it | DEFER — no implementation to seed a fixture against yet |
| `workcenter_queue` | P0 | H — page has no meaningful domain implementation behind it | DEFER — no implementation to seed a fixture against yet |
| `fleet_operations_board` | P1 | G — renderer is only a generic shell with no page-specific … | STRENGTHEN or CONSOLIDATE — confirm this page has page-spec… |
| `alert_board` | P2 | G — renderer is only a generic shell with no page-specific … | STRENGTHEN or CONSOLIDATE — confirm this page has page-spec… |
| `count_session` | P2 | H — page has no meaningful domain implementation behind it | DEFER — no implementation to seed a fixture against yet |
| `crossdock_workspace` | P2 | H — page has no meaningful domain implementation behind it | DEFER — no implementation to seed a fixture against yet |
| `cycle_count_plans` | P2 | H — page has no meaningful domain implementation behind it | DEFER — no implementation to seed a fixture against yet |
| `device_health_board` | P2 | G — renderer is only a generic shell with no page-specific … | STRENGTHEN or CONSOLIDATE — confirm this page has page-spec… |
| `downtime_board` | P2 | H — page has no meaningful domain implementation behind it | DEFER — no implementation to seed a fixture against yet |
| `employee_ui` | P2 | H — page has no meaningful domain implementation behind it | DEFER — no implementation to seed a fixture against yet |
| `extension_installations` | P2 | G — renderer is only a generic shell with no page-specific … | STRENGTHEN or CONSOLIDATE — confirm this page has page-spec… |
| `lot_serial_traceability` | P2 | H — page has no meaningful domain implementation behind it | DEFER — no implementation to seed a fixture against yet |
| `operational_performance` | P2 | H — page has no meaningful domain implementation behind it | DEFER — no implementation to seed a fixture against yet |
| `production_large_screen` | P2 | G — renderer is only a generic shell with no page-specific … | STRENGTHEN or CONSOLIDATE — confirm this page has page-spec… |
| `putaway_rules` | P2 | H — page has no meaningful domain implementation behind it | DEFER — no implementation to seed a fixture against yet |
| `replenishment_rules` | P2 | H — page has no meaningful domain implementation behind it | DEFER — no implementation to seed a fixture against yet |
| `route_health` | P2 | H — page has no meaningful domain implementation behind it | DEFER — no implementation to seed a fixture against yet |
| `sales_price_lists` | P2 | H — page has no meaningful domain implementation behind it | DEFER — no implementation to seed a fixture against yet |
| `scenario_planner` | P2 | H — page has no meaningful domain implementation behind it | DEFER — no implementation to seed a fixture against yet |
| `service_queue_board` | P2 | G — renderer is only a generic shell with no page-specific … | STRENGTHEN or CONSOLIDATE — confirm this page has page-spec… |
| `staging_board` | P2 | H — page has no meaningful domain implementation behind it | DEFER — no implementation to seed a fixture against yet |
| `supplier_portal` | P2 | H — page has no meaningful domain implementation behind it | DEFER — no implementation to seed a fixture against yet |
| `variance_review` | P2 | H — page has no meaningful domain implementation behind it | DEFER — no implementation to seed a fixture against yet |
| `warehouse_large_screen` | P2 | G — renderer is only a generic shell with no page-specific … | STRENGTHEN or CONSOLIDATE — confirm this page has page-spec… |
| `warehouse_topology` | P2 | H — page has no meaningful domain implementation behind it | DEFER — no implementation to seed a fixture against yet |
| `zone_bin_management` | P2 | H — page has no meaningful domain implementation behind it | DEFER — no implementation to seed a fixture against yet |

## Golden Workshop Dataset coverage

Strong, cross-linked coverage: Workshop Jobs (11 rows covering every board state —
new/backlog, due-today, overdue+SLA-breached, blocked, design-approval-pending,
material-shortage+SLA-breached, active-production, quality-hold+SLA-breached,
rework, ready-for-delivery, closed), Warehouse (receiving+discrepancy, lot-controlled
+near-expiration stock, pick tasks, replenishment proposal), Production (BOM, work
centers, work orders, a machine-breakdown downtime event), Quality (an NCR), and the
Commercial/SaaS platform layer.

**Gap found**: zero fixture rows for customers, sales quotations, or suppliers/
purchase orders, despite the canonical schema existing for all three
(`database/migrations/039_crm_sales_contracts_commissions.mjs`,
`040_suppliers_procurement_threeway_match.mjs`). This is why `sales_price_lists`
and `supplier_portal` currently read as "no domain implementation" — the schema is
there, the fixture never points a customer or quotation at it. See
OWNER_PRODUCT_DECISIONS.md §4 for the scoping call.

