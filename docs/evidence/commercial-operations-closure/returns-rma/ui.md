See `../page-register.md` — new "RMA (النظام المعتمد)" tab in
`modules/warranty-rma.js`: list, create form, detail view with
lifecycle-transition buttons for every status, line table, timeline table.
Old "claims" tab relabelled "المطالبات (محلي)" to disambiguate from the new
canonical tab rather than silently left ambiguous — see
`../duplicate-authority-retirement.md` for why it was not removed this wave.
