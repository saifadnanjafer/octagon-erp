# BUILD-13 Runtime Error Ledger

Generated by `scripts/product/build-backlog.mjs` from `docs/product/PAGE_RUNTIME_INSPECTION.json` (2026-08-22T05:05:17.021Z).

Errors are classified by observed shape, never suppressed. The review fixture is a
small permission-scoped dataset, so a 401/403 on a background resource is genuinely
expected — separating those is what makes the real defects visible instead of buried.

**Actionable classes** (must be fixed to close BUILD-13): `SERVER_ERROR`,
`BROKEN_REQUIRED_ENDPOINT`, `UNHANDLED_EXCEPTION`, `UNKNOWN`.
**Non-actionable by design**: `EXPECTED_AUTHORIZATION` — fixing these by weakening
authorization is explicitly forbidden.

## Failed requests by classification (unique endpoint+status)

| Classification | Unique | Actionable |
|---|---|---|

## Unique failed requests

| Status | Method | Path | Occurrences | Pages | Classification |
|---|---|---|---|---|---|

## Unique console errors

| Classification | Unique |
|---|---|

| Occurrences | Pages | Classification | Message (normalized) |
|---|---|---|---|

## Uncaught page exceptions

None observed across the inspected pages.

