task: T1.2
lane: A
agent: claude-sonnet-5
started: 2026-07-12T15:30:00Z
status: DONE
gates: A pass (85 JS files). C (route/boot) verified live. D not run this
  pass (workshop stabilization 12/12 — deferred, unrelated surface). E:
  runAttendanceForecastRegressionTests() 6/6 before AND after (frozen-zone
  canary, clean). F pass (clean boot both before/after, employees=26
  intact). G pass. Manual bad-write test from console: confirmed (see
  notes) — warn-only in choke-point 1, hard-block in choke-point 2.
notes: |
  Both choke points wired: PentagonDB.mutate() (services/auditService.js,
  new _checkSchemaViolations() helper) and saveData() (app.js, employees
  protect:true hard-block). ENFORCE stays false (warn-first) by default —
  this commit only ever LOGS violations for non-employees collections
  today; nothing is actually rejected except empty employees writes
  (which were already effectively forbidden by policy, just not
  previously enforced at this exact point).

  Verified live against the running dev server, not just node --check:
  - Canary 6/6 unchanged.
  - saveData(): swapped the real `employees` global to [] for one
    synchronous tick, confirmed zero fetch() calls fired, restored
    immediately — never touched the server.
  - PentagonDB.mutate(): called _checkSchemaViolations() directly against
    a synthetic malformed account_moves record — confirmed warn-only
    (ENFORCE=false), ring buffer recorded full detail, real cache
    untouched.

  SIGNIFICANT LIVE FINDING: on a plain page reload during this session (no
  test code), the new saveData() guard fired TWICE against genuine
  premature empty-employees write attempts during boot and blocked both —
  real evidence of a boot-race the existing 3-layer employee protection
  doesn't fully cover. employees still ended up populated correctly (26)
  and nothing broke. NOT root-caused or fixed here — that's a separate,
  deeper investigation into the boot sequence's timing, out of T1.2's
  mechanical "add the validation layer" scope. Flagged for the owner /a
  future debugging session.
