task: T2.2
lane: A
agent: claude-opus-4-8
started: 2026-07-13T10:45:00Z
status: DONE
gates: |
  A pass (315 JS files). E/trial-balance pass: on the isolated 8090 copy a
  crafted mis-journaled customer_charge move was repaired and sum(debits)-
  sum(credits) over posted account_moves stayed 0 before AND after (the
  reversal + corrected move are each self-balanced). F pass (live reload,
  fresh app.js v22, ZERO console errors, repair button renders in the finance
  journal-entries tab). G pass. Create-path verify: addFinanceTransaction
  customer_charge auto-synced to a v6 move in j_sale debiting
  receivables_customers / crediting income_sales.
notes: |
  Two parts — routing already fixed, repair function added.

  ROUTING (already correct on read): syncLegacyTransactionToV6 (app.js) already
  routes customer_charge to journal j_sale, debit receivables_customers, credit
  income_sales (the plan's "expense-branch" bug was fixed at some earlier
  point). Verified live create-path on the 8090 copy: CORRECT=true.

  REPAIR (new, app.js — my file, add-only):
  - repairCustomerChargeMoves({dryRun?}) — idempotent, MANUAL. Scans posted
    account_moves whose origin is legacy_sync/<txId> where the legacy tx is
    customer_charge but the move is NOT (j_sale AND debiting AR). For each:
    FinanceService.cancelMove (dated reversal — never mutates a posted move in
    place) then re-post a correct j_sale/AR move, and relink tx.v6_move_id +
    saveData(). Returns {scanned,candidates,repaired,skipped,details}.
  - runCustomerChargeRepair() — confirm-gated UI handler; button
    «إصلاح قيود العملاء» added to the finance journal-entries tab.
  - window.repairCustomerChargeMoves / window.runCustomerChargeRepair exposed.

  VERIFIED on 8090 copy (permissions stubbed on the throwaway copy to exercise
  the finance path, not auth; never live for mutations):
  1. Crafted a wrong j_purc/expense move for a customer_charge tx -> dry-run
     found 1 candidate -> repair reversed it (original state=cancel) and posted
     a correct j_sale/AR move, tx relinked. Trial balance diff = 0 before & after.
  2. IDEMPOTENT: second dry-run found 0 candidates.
  3. LOCKED-PERIOD SAFETY: a mis-journaled move dated inside a locked period is
     reported skipped («الفترة مقفلة: لا يمكن إلغاء…») and left untouched
     (still posted) — the repair respects T2.1 and never force-modifies locked
     history (unlocking is an owner decision).

  LIVE STATE: 526 legacy txs (51 income / 240 expense / 235 advance) — ZERO
  customer_charge present, so 0 mis-journaled moves on live; live dry-run
  scanned 568 moves, 0 candidates. The repair is a safe no-op on current data
  and a ready tool if historical/imported customer_charge moves ever surface.
  No live server restart needed (client-only change; app.js ?v= bumped to v22).
