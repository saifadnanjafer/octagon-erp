task: T1.3
lane: A
agent: claude-sonnet-5
started: 2026-07-12T16:00:00Z
status: CLAIMED
gates: —
notes: Server-side write guard. Higher risk than T1.1/T1.2 — this is the
  live backend process, not client JS. Will read server.js's existing
  preserve-allowlist logic first (build on it, not replace), then verify
  via an isolated 8090 copy-server (copy database.db + database.json to a
  scratch dir, run a second node server.js instance against the copy on
  port 8090, curl malicious payloads at THAT, tear down cleanly) — never
  test destructive payloads against the live server on :8080.
