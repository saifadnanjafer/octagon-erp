task: T1.4
lane: A
agent: claude-opus-4-8
started: 2026-07-13T09:45:00Z
status: DONE
gates: |
  A pass (315 JS files). Unit/race pass: two parallel next('inv') on the
  isolated 8090 copy returned distinct INV-2026-00001/00002; a 25-way
  concurrent next('tkt') burst returned 25 unique numbers 1..25, zero
  duplicates (BEGIN IMMEDIATE transaction proven under load). F pass (live
  server restarted with the new code, fresh-tab clean boot with ZERO
  console errors, employees=26 via JarvisBrain.employeeList). E pass
  (attendance canary runAttendanceForecastRegressionTests 6/6 post-restart).
  G pass. Live e2e: OctagonSeq.next('_smoke') x2 in-browser returned
  server-issued (non-OFFLINE) distinct numbers; peek non-consuming confirmed.
notes: |
  modules/sequence-service.js (client OctagonSeq.next/peek) + server.js
  (sequences SQLite table, issueNextSequence() with BEGIN IMMEDIATE
  transaction, POST /api/sequence/next + GET /api/sequence/peek, ECC
  {ok,data,error} envelope, apiProtectionMatrix rows). Wired into index.html
  before app.js. Add-only: no existing numbering call site touched (NEW
  documents only, per plan adoption rule).

  DESIGN: one row per code; next_number issued+incremented inside the
  transaction so concurrent acquires never collide. Yearly reset via a `year`
  column (INV-2026-... -> INV-2027-00001). Known codes (inv/bill/job/tkt/sr/
  po/so/badge/quote/sub) have default prefix+padding; unknown codes honoured
  with prefix=CODE.toUpperCase(), padding=4, and one-time caller override of
  prefix/padding. Client OFFLINE- fallback (localStorage per-code counter,
  same format, tagged OFFLINE-) on any non-200 so numbers are never silently
  reused.

  SESSION-START DUTY: applied the 3 pending integration-queue items (T6.1
  system-settings css/js, T6.2 repo.js) to index.html + bumped app.js ?v=;
  verified live (Repo object present, system_settings page renders ss-shell
  page-active). Commit 8d289a6.

  INCIDENTAL FIX (app.js, LANE-A file): wiring T6.1 surfaced a repeated
  console 404 — the view-template loader fetched /views/system_settings.html
  for the JS-self-rendered settings page. Added a JS_RENDERED_PAGES skip-set
  ({import_center, system_settings}) at the top of ensurePageTemplateLoaded
  plus the system_settings->pageSystemSettings pageMap entry. This also
  resolves the pre-existing known import_center template-fetch race noted in
  §13's Phase-3 row. Verified: instrumented fetch shows 0 /views/
  system_settings.html requests on navigation, page still renders; fresh-tab
  boot console is clean.

  VERIFICATION (isolated copy, never live): copied database.db(+wal/shm) +
  .env to scratch, ran PORT=8090 OCTAGON_DB_FILE/OCTAGON_SQLITE_DB_FILE/
  OCTAGON_BACKUP_DIR overrides -> node server.js against the copy only; ran
  all endpoint tests there; killed the 8090 process, removed scratch +
  verify.lock. Then restarted the live :8080 server with the new code (normal
  deploy step, same as T1.3) and did the browser e2e/Gate E/F above.

  MINOR ARTIFACT: one inert `_smoke` row exists in the LIVE sequences table
  from the in-browser e2e (throwaway code, never used by any real document —
  no delete endpoint by design, left as-is rather than raw-poking live DB).
  Real business codes (inv/job/...) remain unissued/pristine.
