task: T1.5
lane: A
agent: claude-opus-4-8
started: 2026-07-13T10:05:00Z
status: CLAIMED
gates: —
notes: |
  Nightly backup create -> verify -> prune(keep 14) -> log to server-backup.log.
  FINDING: createDatabaseBackup() in server.js ALREADY creates + verifies
  (verifyBackupAgainstLive throws + deletes on mismatch). The Phase-3 scheduler
  (T3.1, server-scheduler.js, LANE-B1 file) already runs a nightly_backup_verify
  job that calls ctx.createDatabaseBackup('scheduler'). Gaps vs T1.5 spec:
  prune-to-14 + server-backup.log logging. Implementing a tag-scoped nightly
  cycle wrapper in server.js (my file) and passing it into the scheduler ctx —
  no scheduler edit needed. Tag-scoped prune (scheduler-tagged only) so the
  existing hourly auto-backup keep-30 retention is NOT regressed.
