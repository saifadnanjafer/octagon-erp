task: T1.1
lane: A
agent: claude-sonnet-5
started: 2026-07-12T15:00:00Z
status: DONE
gates: A pass (85 JS files); C/F verified via live browser session against
  the already-running dev server (schema loaded, ENFORCE=false default,
  zero console errors, all 25 non-nested collections validated with zero
  record errors against real live data). G pass (clean commit).
notes: modules/schema-registry.js created and wired into index.html before
  app.js. 25 collection schemas (employees protect:true + 6 core omni
  collections + 15 vertical-module collections + 3 legacy/v6 finance
  collections) + 1 nested marker (omni.taskManager). Every path verified
  against the running server's live state, not assumed from the plan's
  illustrative example (which was wrong on 2 points: customers live at
  finance.customers not omni.customers; employees is a bare top-level
  global not omni.employees).

  FINDING surfaced, not fixed: omni.jobOrders is completely absent from
  live state (omni.__jobOrdersMigrated=true but the array itself was never
  created/persisted) — flagged for the owner in the commit message.

  NOT done here (future tasks): wiring validate()/validateCollection() into
  any actual write path (T1.2 — PentagonDB.mutate + saveData() choke
  points), or the server-side guard (T1.3). This commit is schema
  definitions + a validation API only, per T1.1's own scope.
