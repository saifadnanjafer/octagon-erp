task: T2.2
lane: A
agent: claude-opus-4-8
started: 2026-07-13T10:45:00Z
status: CLAIMED
gates: —
notes: |
  customer_charge v6 bridge. FINDING on read: syncLegacyTransactionToV6
  (app.js L39762) ALREADY routes customer_charge correctly — journal j_sale,
  debit receivables_customers, credit income_sales (L39771-39785). So the
  routing bug is already fixed. Remaining T2.2 work: the idempotent
  repairCustomerChargeMoves() manual-trigger repair for any HISTORICAL v6
  moves mis-journaled before the routing fix (reverse via cancelMove +
  re-post correctly; never mutate posted moves in place). Scanning live data
  read-only first to size the repair need, then implementing the function +
  finance-settings trigger. Verify create-path on 8090 copy + trial balance.
