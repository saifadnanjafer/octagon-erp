# دليل تسليم التصميم — Octagon ERP (UI Designer Handoff)

> الهدف: تمكين مصمم UI من إعادة تنسيق/تطوير الواجهة **دون كسر المنطق البرمجي**.
> التاريخ: 2026-06-24 · الحالة: وظيفياً مستقر (Route Health 86/86 · الاستبيان الوظيفي: 850/852 زر سليم).

---

## 1. ما هو النظام
- نظام ERP عربي كامل (RTL) للورشة + المخزون + الرواتب + المالية + 80+ صفحة/وحدة.
- **بلا framework**: HTML + CSS + JavaScript عادي (vanilla). لا React/Vue.
- خادم محلي بسيط: `node server.js` على المنفذ 8080.
- البيانات في `database.json` / `database.db` محلياً.

## 2. كيف تشغّله محلياً (للمعاينة)
```bash
cd octagon-erp
node server.js
# افتح المتصفح: http://localhost:8080
```

## 3. معمارية الواجهة (مهم للمصمم)
- **`index.html`** = القشرة (sidebar + topbar + #mainContent + modal overlay). تحتوي روابط كل ملفات CSS وترتيب تحميلها.
- **`views/*.html`** = 86 ملف، كل صفحة في ملف مستقل، تُحمَّل **عند الطلب** (lazy) داخل `#mainContent`.
- كل صفحة عنصر `<section class="page" id="pageXxx">`. الصفحة الظاهرة تأخذ كلاس **`.page-active`** (الباقي `display:none`).
- التنقّل عبر `switchPage('key')` (أزرار الـ sidebar فيها `data-page="key"`).

## 4. نظام الألوان والتوكنز (نقطة دخول التصميم) ⭐
كل الألوان/الخطوط/الزوايا معرّفة كـ **CSS variables** في أعلى `style.css` (`:root`). **عدّل هنا أولاً** ليتغير النظام كله:

| التوكن | القيمة | الاستخدام |
|---|---|---|
| `--bg-primary` | `#0a0e1a` | خلفية رئيسية |
| `--bg-card` | `rgba(17,24,39,.7)` | خلفية البطاقات (زجاجي) |
| `--text-primary` | `#f1f5f9` | نص رئيسي |
| `--text-secondary` | `#94a3b8` | نص ثانوي |
| `--accent-blue` | `#38bdf8` | اللون الأساسي |
| `--accent-green/red/yellow` | … | حالات نجاح/خطر/تنبيه |
| `--gradient-primary` | أزرق→بنفسجي | أزرار/عناوين |
| `--radius-sm/md/lg/xl` | 8/12/16/20px | الزوايا |
| `--font-ar` | Tajawal/Noto/Cairo | الخط العربي |
| `--shadow-sm/md/lg` | … | الظلال |
| `--transition` | `.3s cubic-bezier` | الحركات |

> الثيم الحالي: **Dark Glassmorphism**. توجد ثيمات بديلة جزئية في `style.css` (حول السطر 2307، 2460) و`glass-real.css`.

## 5. ملفات CSS وترتيب التحميل
- **`style.css`** (≈422KB) = الملف الرئيسي + كل التوكنز + غالبية المكوّنات. هذا أهم ملف.
- مساعدات عامة: `omni-ux-v2.css`, `ui-contrast-fix.css`, `glass-*.css`, `layout-improved.css`, `force-horizontal.css`.
- **CSS لكل وحدة**: `modules/*.css` (pos, pharmacy, verticals, mrp, helpdesk … ~35 ملف). كل وحدة تنسّق صفحتها.
- الترتيب في `index.html` (السطور 22–61). الأخير يفوز عند التعارض.

## 6. جرد المكوّنات (Component Inventory)
| المكوّن | الكلاسات | ملاحظة |
|---|---|---|
| أزرار أساسية | `.btn .btn-primary` / `.btn-secondary` / `.btn-sm` | الأكثر استخداماً |
| أزرار وحدات Enterprise | `.ent-btn .ent-btn.primary` | صفحات الخزينة/الذمم/العقود… |
| أزرار وحدات أخرى | لكل وحدة بادئتها (`.ph-btn`, `.rfq-…`, `.p6a-btn`, `.risk-btn`…) | **غير موحّدة** — انظر §8 |
| بطاقات | `.card`, `.ent-panel`, `.kpi`/`.ent-kpi-grid`, `.stat-card` | |
| جداول | `.ent-table`, `.p6a-table`, جداول style.css | |
| تبويبات | `.tab`, `.recon-tab-bar`, `.task-view-tabs` | |
| Modal | `#omniModalOverlay` (عبر `showOmniModal()`) | لا تغيّر الـ id |

## 7. قواعد ذهبية — ماذا يلمس المصمم وماذا لا ⚠️

**✅ آمن للمصمم:**
- كل ملفات `.css` (الألوان، المسافات، الظلال، الخطوط، الـ layout).
- توكنز `:root` في `style.css`.
- النص الظاهر والكلاسات داخل `views/*.html` (مع الحفاظ على البنية).

**🚫 ممنوع لمسه (يكسر المنطق):**
- أي `id=` أو `data-page=` أو `data-*` — الكود يعتمد عليها للعثور على العناصر.
- محتوى `onclick="..."` وأسماء الدوال داخلها.
- ملفات `.js` (app.js والوحدات) — هذه تبني HTML برمجياً؛ تغيير الكلاسات هنا يحتاج مطوّراً لا مصمماً.
- الاتجاه `dir="rtl"` وخصائص RTL.

> ملاحظة مهمة: **جزء كبير من الـ HTML مولّد من JavaScript** (داخل دوال `render*` في `app.js` و`modules/*.js`)، وليس في `views/*.html`. لتغيير شكل تلك العناصر، يعدّل المصمم **الكلاسات في CSS** (لا الـ HTML المولّد). إن احتاج تغيير بنية عنصر مولّد، يلزم تنسيق مع مطوّر.

## 8. ديون التصميم المعروفة (للمصمم أن ينتبه لها)
1. **توحيد الأزرار**: 3 أنظمة أزرار متوازية (`.btn`, `.ent-btn`, وأزرار-لكل-وحدة). توصية: توحيدها على نظام واحد عبر التوكنز.
2. **تسرّب CSS عالمي**: بعض ملفات الوحدات عرّفت سيلكتورات عامة (مثل `.btn-secondary`) سبّبت تعارضات سابقة (أُصلحت في `ui-contrast-fix.css`). عند إضافة CSS، **اجعله مُنطّقاً تحت كلاس الصفحة** لتجنّب التسرّب.
3. **حجم `style.css`** (422KB) — يستفيد من تقسيم/تنظيم.
4. **التباين والثيم الفاتح**: النظام مصمَّم داكناً؛ إن أُريد ثيم فاتح، يُبنى عبر طبقة توكنز بديلة.

## 9. الطريقة الموصى بها للمصمم
1. شغّل النظام وتصفّح الـ86 صفحة (sidebar).
2. ابدأ من **توكنز `:root`** لضبط الهوية البصرية (ألوان/خط/زوايا) — تنعكس فوراً على كل النظام.
3. وحّد نظام الأزرار والبطاقات عبر التوكنز.
4. عالج ديون §8 تدريجياً.
5. اختبر دائماً على **RTL عربي** وعلى الشاشات الصغيرة (موبايل/كشك/شاشة TV — النظام يُستخدم على هذه الأجهزة).

---
*بعد انتهاء المصمم: تبقى بنود ما-قبل-الرفع الوظيفية (المصادقة بكلمات مرور، HTTPS، تدوير مفاتيح API، اختبار الأجهزة) — موثّقة في `HERE.md` و`LAUNCH_AUDIT.md`.*
