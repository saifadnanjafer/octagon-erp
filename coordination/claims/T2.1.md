task: T2.1
lane: A
agent: claude-opus-4-8
started: 2026-07-13T10:25:00Z
status: DONE
gates: |
  A pass (no product-code change; verification-only). Enforcement pass on the
  isolated 8090 copy driving FinanceService directly (permissions stubbed on
  the throwaway copy to exercise the lock path, not auth): with lock=2026-06-30,
  createMove(2026-05-15) REJECTED «الفترة مقفلة: لا يمكن إنشاء/تعديل حركة
  بتاريخ 2026-05-15»; createMove(2026-07-15) SUCCEEDED; createMove(2026-06-30)
  REJECTED (<= is inclusive); then lock moved to 2026-07-31 and cancelMove of
  the 2026-07-15 move REJECTED «الفترة مقفلة: لا يمكن إلغاء حركة بتاريخ
  2026-07-15»; setLockDate persisted through PentagonDB.mutate and reset to
  empty cleanly. C/E/F unaffected (zero code change). G pass (this doc commit).
notes: |
  FISCAL PERIOD LOCK ALREADY EXISTS AND IS FULLY ENFORCED — the plan's T2.1
  was already satisfied by the shipped v6 finance system. No product code
  written; this task is a verification + audit-trail closure.

  Evidence (services/financeService.js):
  - isLocked(db,date) = !!db._lock_date && date <= db._lock_date (inclusive).
  - assertNotLocked(db,date,action) throws «الفترة مقفلة: لا يمكن <action>
    حركة بتاريخ <date>».
  - createMove (L590), updateMove (L609/611), postMove (L640), cancelMove
    (L661) all call assertNotLocked before mutating. cancelMove additionally
    creates its reversal via createMove, so a reversal dated into a locked
    period is independently blocked too.
  - setLockDate (L724-726) stores db._lock_date via PentagonDB.mutate.
  UI (app.js): journal-entries tab shows a «تاريخ الإقفال» field + save/close/
  reopen buttons (L29728-29733), a 🔒 badge per locked row (L29709), and a
  lock-date chip (L5070). Manager-only override intentionally absent (owner is
  sole manager; unlocking = editing the date) — matches the plan.

  MINOR WORDING NOTE (not changed, add-only discipline): the existing Arabic
  error is «الفترة مقفلة: لا يمكن … حركة بتاريخ <date>» rather than the plan's
  illustrative «الفترة المالية مقفلة حتى …». Functionally correct and Arabic;
  left as-is to avoid churning proven finance code.

  Verified on an isolated 8090 copy per §3.8 (never live — creating/posting
  test moves is a real mutation); copy + verify.lock torn down after.
