# Owner Consolidation Decisions

Status: **human decision required**. Navigation Recovery 1 made no irreversible deletion or merge.

Approve, revise, or reject each proposed consolidation:

- [ ] Retain `calculator` as TAB under `timesheet`.
- [ ] Retain `kanban` as TAB under `task_manager`.
- [ ] Retain `locations` as TAB under `warehouses`.
- [ ] Retain `pos_deepening` as ALIAS under `pos`.
- [ ] Retain `workshop_tv` as TAB under `task_manager`.

- [ ] Decide whether orphan `credit_collections` should remain a compatibility surface, move under its canonical owner, or be retired through a separate approved change.
- [ ] Decide whether orphan `electronic_signatures` should remain a compatibility surface, move under its canonical owner, or be retired through a separate approved change.
- [ ] Decide whether orphan `sales_commissions` should remain a compatibility surface, move under its canonical owner, or be retired through a separate approved change.
- [ ] Decide whether orphan `service_contracts` should remain a compatibility surface, move under its canonical owner, or be retired through a separate approved change.
