task: T2.3
lane: A
agent: claude-opus-4-8
started: 2026-07-13T11:25:00Z
status: CLAIMED
gates: —
notes: |
  change-tracker.js (chatter-lite). New modules/change-tracker.js:
  TrackChanges.record(collection, id, patch, actor) -> omni.changeLog ring
  buffer (cap 5000, oldest evicted). Wire into PentagonDB.mutate's T1.2
  validation wrapper (pentagondb-layer) + admin CRUD save paths (omni-layer).
  «سجل التغييرات» drawer component callable from record modals; wire into
  finance moves + jobOrders first. FROZEN ZONE EXCLUDED: timesheet/attendance/
  employees payroll edits NOT tracked (existing audit stays). Investigating
  wiring points now.
