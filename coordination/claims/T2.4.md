task: T2.4
lane: A
agent: claude-opus-4-8
started: 2026-07-13T11:05:00Z
status: CLAIMED
gates: —
notes: |
  Trial-balance assertion. New modules/finance-selftest.js exposing
  window.runTrialBalanceCheck(): read-only, sum(debits)-sum(credits) over
  account_moves must be 0, plus per-move balance check and suspense-line
  count (report only — historical suspense is an owner finding, not an
  auto-fix). Wired into index.html. No central test runner exists yet (T5.1
  system_check is LANE-D1, pending) so the function is exposed on window for
  that runner to pick up. Verify on live read-only (imbalance must be 0);
  record finding in §13 if not.
