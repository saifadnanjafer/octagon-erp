# Octagon Navigation Map

The primary sidebar contains 231 verified workspaces arranged into seven top-level domains. Each group is collapsed by default except the active group; only one domain is visible at a time.

### finance / finance_consolidation

- fa-chart-pie · ربط الحسابات — Account Mapping (`account_mapping`)
- fa-chart-pie · التقارير الموحدة — Consolidated Reports (`consolidated_reports`)
- fa-chart-pie · مجموعات التوحيد — Consolidation Groups (`consolidation_groups`)
- fa-chart-pie · تتبع التوحيد — Consolidation Lineage (`consolidation_lineage`)
- fa-chart-pie · عمليات التوحيد — Consolidation Runs (`consolidation_runs`)
- fa-chart-pie · قيود الاستبعاد — Eliminations (`eliminations`)

### admin / admin_org

- fa-shield-halved لوحة تحكم الأدمن — Admin Panel (`admin_panel`)
- fa-shield-halved جودة البيانات — Data Quality (`data_quality`)
- fa-shield-halved جاهزية التشغيل — Deploy Ready (`deploy_ready`)
- fa-shield-halved مركز الأجهزة وإنترنت الأشياء — Device Center (`device_center`)
- fa-shield-halved لوحة الموظف — Employee Ui (`employee_ui`)
- fa-shield-halved استيراد البيانات — Data Import (`import_center`)
- fa-shield-halved مركز التكامل — Integration Hub (`integration_hub`)
- fa-shield-halved الفروع والعملات — Multi Entity (`multi_entity`)
- fa-shield-halved المخاطر والامتثال — Risk Compliance (`risk_compliance`)
- fa-shield-halved التدقيق والأمن — Security Center (`security_center`)
- fa-shield-halved فحص النظام — System Check (`system_check`)
- fa-shield-halved إعدادات النظام — System Settings (`system_settings`)
- fa-shield-halved التدريب والتعلم — Training Lms (`training_lms`)

### intelligence / intelligence_ai

- fa-robot مساعد الذكاء الاصطناعي — AI Assistant (`ai_assistant`)
- fa-robot مصنع التطوير — Ai Factory (`ai_factory`)
- fa-robot نظرة عامة على الذكاء الاصطناعي — AI Overview (`ai_overview`)
- fa-robot طابور الذكاء — Ai Queue (`ai_queue`)
- fa-robot حالة الذكاء الصناعي — Ai Status (`ai_status`)
- fa-robot سجل أدوات الذكاء — Ai Tools (`ai_tools`)
- fa-robot مخطط سيناريوهات الذكاء — Scenario Planner (`scenario_planner`)

### intelligence / intelligence_governance

- fa-shield-halved مصادر سياق الذكاء الاصطناعي — AI Context Sources (`ai_context_sources`)
- fa-shield-halved سجل سياسات الذكاء الاصطناعي — AI Policy Registry (`ai_policy_registry`)
- fa-shield-halved قوالب أوامر الذكاء الاصطناعي — AI Prompt Templates (`ai_prompt_templates`)
- fa-shield-halved صندوق مقترحات الذكاء الاصطناعي — AI Proposal Inbox (`ai_proposal_inbox`)
- fa-shield-halved سجل تشغيل الذكاء الاصطناعي — AI Run History (`ai_run_history`)

### ops / ops_boards

- fa-table-columns لوحة التنبيهات — Alert Board (`alert_board`)
- fa-table-columns لوحة صحة الأجهزة — Device Health Board (`device_health_board`)
- fa-table-columns لوحة عمليات الأسطول — Fleet Operations Board (`fleet_operations_board`)
- fa-table-columns شاشة الإنتاج الكبيرة — Production Large Screen (`production_large_screen`)
- fa-table-columns لوحة طابور الخدمة — Service Queue Board (`service_queue_board`)
- fa-table-columns شاشة المستودع الكبيرة — Warehouse Large Screen (`warehouse_large_screen`)

### intelligence / intelligence_core

- fa-chart-line التحليلات والذكاء — Analytics (`analytics`)
- fa-chart-line محرك الأتمتة — Automation (`automation`)
- fa-chart-line لوحة الذكاء — Intelligence (`intelligence`)
- fa-chart-line التقارير الذكية — Nl Reports (`nl_reports`)
- fa-chart-line فحص صحة النظام — Route Health (`route_health`)
- fa-chart-line بوابة Telegram — Telegram (`telegram`)
- fa-chart-line استقبال واتساب — Whatsapp (`whatsapp`)

### commercial / commercial_relationships

- fa-handshake-angle المواعيد والحجوزات — Appointments (`appointments`)
- fa-handshake-angle بوابة العميل — Customer Portal (`customer_portal`)
- fa-handshake-angle خدمة العملاء — Helpdesk (`helpdesk`)
- fa-handshake-angle مشاركة وإرسال — Omni Communications (`omni_communications`)
- fa-handshake-angle العقود والموافقات — Sales Contracts (`sales_contracts`)
- fa-handshake-angle الاشتراكات — Subscriptions (`subscriptions`)
- fa-handshake-angle الضمان والمرتجعات — Warranty (`warranty`)

### resources / resources_supply

- fa-truck-fast الموافقات — Approvals (`approvals`)
- fa-truck-fast العقود والشؤون القانونية — Contracts (`contracts`)
- fa-truck-fast اللوجستيات والتوصيل — Logistics (`logistics`)
- fa-truck-fast المشتريات — Procurement (`procurement`)
- fa-truck-fast المشاريع — Projects (`projects`)
- fa-truck-fast بوابة الموردين — Supplier Portal (`supplier_portal`)

### finance / finance_accounts

- fa-file-invoice-dollar الذمم المدينة والدائنة — Ar Ap (`ar_ap`)
- fa-file-invoice-dollar البنوك والخزينة — Banking (`banking`)
- fa-file-invoice-dollar الموازنات — Budgeting (`budgeting`)
- fa-file-invoice-dollar قاصة الورشة — Cashbox (`cashbox`)
- fa-file-invoice-dollar أرصدة العملاء — Customers (`customers`)
- fa-file-invoice-dollar المصروفات — Expenses (`expenses`)
- fa-file-invoice-dollar الداشبورد المالي — Finance (`finance`)
- fa-file-invoice-dollar خطط التقسيط — Finance Installments (`finance_installments`)
- fa-file-invoice-dollar الواردات — Income (`income`)
- fa-file-invoice-dollar الضرائب والفوترة — Tax Compliance (`tax_compliance`)
- fa-file-invoice-dollar محاسبة ودوام الورشة — Workshop Ledger (`workshop_ledger`)

### resources / resources_assets

- fa-truck الأصول والصيانة — Assets (`assets`)
- fa-truck المركبات — Fleet (`fleet`)

### commercial / commercial_marketing

- fa-bullhorn تحليلات الإسناد — Attribution Insights (`attribution_insights`)
- fa-bullhorn الحملات — Campaigns (`campaigns`)
- fa-bullhorn اعتمادات المحتوى — Content Approvals (`content_approvals`)
- fa-bullhorn تقويم المحتوى — Content Calendar (`content_calendar`)
- fa-bullhorn تسجيل حضور الفعالية — Event Check-in (`event_checkin`)
- fa-bullhorn مخطط الفعاليات — Event Planner (`event_planner`)
- fa-bullhorn تسجيلات الفعاليات — Event Registrations (`event_registrations`)
- fa-bullhorn الفعاليات — Events (`events`)
- fa-bullhorn نظرة عامة على الفعاليات — Events Overview (`events_overview`)
- fa-bullhorn التسويق والحملات — Marketing (`marketing`)
- fa-bullhorn نظرة عامة على التسويق — Marketing Overview (`marketing_overview`)

### commercial / commercial_saas

- fa-cloud-arrow-up محاكي الفوترة — Billing Simulator (`billing_simulator`)
- fa-cloud-arrow-up الخطط التجارية — Commercial Plans (`commercial_plans`)
- fa-cloud-arrow-up الاستحقاقات — Entitlements (`entitlements`)
- fa-cloud-arrow-up تثبيت الإضافات — Extension Installations (`extension_installations`)
- fa-cloud-arrow-up سوق الإضافات — Extension Marketplace (`extension_marketplace`)
- fa-cloud-arrow-up نظرة عامة على البرمجيات كخدمة — SaaS Overview (`saas_overview`)
- fa-cloud-arrow-up المقاعد والحدود — Seats and Limits (`seats_and_limits`)
- fa-cloud-arrow-up تفاصيل المستأجر — Tenant Detail (`tenant_detail`)
- fa-cloud-arrow-up دليل المستأجرين — Tenant Directory (`tenant_directory`)
- fa-cloud-arrow-up الاستخدام والحصص — Usage and Quotas (`usage_and_quotas`)

### core / core_daily

- fa-calendar-check تقويم الدوام — Calendar (`calendar`)
- fa-calendar-check مهامي اليوم (موبايل) — Employee Mobile (`employee_mobile`)
- fa-calendar-check الموظفين والأرصدة — Employees (`employees`)
- fa-calendar-check الرئيسية — Home (`home`)
- fa-calendar-check عملي — My Work (`my_work`)
- fa-calendar-check التايم شيت الذكي — Timesheet (`timesheet`)
- fa-calendar-check الرئيسية حسب الدور — Wfl Home (`wfl_home`)

### ops / ops_inventory

- fa-boxes-stacked العمليات الموحّدة — Canonical Operations (`canonical_console`)
- fa-boxes-stacked المخزون القانوني — Canonical Inventory (`canonical_inventory`)
- fa-boxes-stacked معدات الورشة — Equipment (`equipment`)
- fa-boxes-stacked المخزون والمواد — Inventory (`inventory`)
- fa-boxes-stacked العملاء والموردون — Customers &amp; Suppliers (`parties`)
- fa-boxes-stacked المنتجات والمواد — Products &amp; Materials (`products`)
- fa-boxes-stacked مركز الجودة — Qc Center (`qc_center`)
- fa-boxes-stacked هيكل المستودع — Warehouse Topology (`warehouse_topology`)
- fa-boxes-stacked المستودعات — Warehouses (`warehouses`)
- fa-boxes-stacked إدارة المناطق والخانات — Zone and Bin Management (`zone_bin_management`)

### commercial / commercial_verticals

- fa-store العيادة — Clinic (`clinic`)
- fa-store الخدمة الميدانية — Field Service (`field_service`)
- fa-store الفندق — Hotel (`hotel`)
- fa-store الصيدلية — Pharmacy (`pharmacy`)
- fa-store العقارات — Real Estate (`real-estate`)
- fa-store تأجير المعدات — Rental (`rental`)
- fa-store المطعم — Restaurant (`restaurant`)
- fa-store المتجر — Retail (`retail`)
- fa-store حزم القطاعات — Vertical Packs (`vertical_packs`)
- fa-store إعداد حزمة الورشة — Workshop Pack Setup (`workshop_pack_setup`)

### ops / ops_control

- fa-diagram-project مركز القيادة — Command Center (`command_center`)
- fa-diagram-project مكتبة SOP — Sop (`sop`)
- fa-diagram-project إدارة المهام — Task Manager (`task_manager`)
- fa-diagram-project مصمم العمليات — Workflow (`workflow`)
- fa-diagram-project مركز قيادة الورشة — Workshop Command Center (`workshop_command_center`)
- fa-diagram-project جاهزية الورشة — Workshop Readiness (`workshop_readiness`)

### resources / resources_people

- fa-users-gear ملفات الكفاءات — Competency Profiles (`competency_profiles`)
- fa-users-gear خطط التطوير — Development Plans (`development_plans`)
- fa-users-gear التعلم والشهادات — Learning & Certifications (`learning_and_certifications`)
- fa-users-gear نظرة عامة على تطوير الأفراد — People Development (`people_development_overview`)
- fa-users-gear التوظيف والإجازات — People Ops (`people_ops`)
- fa-users-gear أدلة المهارات — Skill Evidence (`person_skill_evidence`)
- fa-users-gear دليل المهارات — Skills Catalog (`skills_catalog`)

### resources / resources_devices

- fa-microchip ملفات الإعداد — Configuration Profiles (`configuration_profiles`)
- fa-microchip تنبيهات الأجهزة — Device Alerts (`device_alerts`)
- fa-microchip مركز أوامر الأجهزة — Device Command Center (`device_command_center`)
- fa-microchip تفاصيل الجهاز — Device Detail (`device_detail`)
- fa-microchip تسجيل الأجهزة — Device Enrollment (`device_enrollment`)
- fa-microchip مركز صحة الأجهزة — Device Health Center (`device_health_center`)
- fa-microchip سجل الأجهزة — Device Registry (`device_registry`)
- fa-microchip دليل البرامج الثابتة — Firmware Catalogue (`firmware_catalogue`)
- fa-microchip إدارة البوابات — Gateway Management (`gateway_management`)
- fa-microchip محاكي النشر — Rollout Simulator (`rollout_simulator`)
- fa-microchip إدارة المستشعرات — Sensor Management (`sensor_management`)
- fa-microchip مستكشف القياسات — Telemetry Explorer (`telemetry_explorer`)

### resources / resources_offline

- fa-cloud-arrow-down حل التعارضات — Conflict Resolution (`conflict_resolution`)
- fa-cloud-arrow-down سياسات العمل دون اتصال — Offline Capability Policies (`offline_capability_policies`)
- fa-cloud-arrow-down سجل عملاء دون اتصال — Offline Client Registry (`offline_client_registry`)
- fa-cloud-arrow-down طابور العمل دون اتصال — Offline Queue (`offline_queue`)
- fa-cloud-arrow-down تعارضات المزامنة — Sync Conflicts (`sync_conflicts`)
- fa-cloud-arrow-down جلسات المزامنة — Sync Sessions (`sync_sessions`)

### ops / ops_warehouse_fulfillment

- fa-clipboard-check جلسة الجرد — Count Session (`count_session`)
- fa-clipboard-check خطط الجرد الدوري — Cycle Count Plans (`cycle_count_plans`)
- fa-clipboard-check الالتقاط المتنقل — Mobile Picking (`mobile_picking`)
- fa-clipboard-check طابور مهام الالتقاط — Pick Task Queue (`pick_task_queue`)
- fa-clipboard-check مراجعة الفروقات — Variance Review (`variance_review`)
- fa-clipboard-check تنفيذ الموجات — Wave Execution (`wave_execution`)
- fa-clipboard-check تخطيط الموجات — Wave Planning (`wave_planning`)

### ops / ops_warehouse_traceability

- fa-route مساحة العبور المباشر — Cross-Dock Workspace (`crossdock_workspace`)
- fa-route تسجيل وصول الرصيف — Dock Check-In (`dock_checkin`)
- fa-route جدول الأرصفة — Dock Schedule (`dock_schedule`)
- fa-route طابور تواريخ الانتهاء — Expiration Queue (`expiration_queue`)
- fa-route تتبع الدفعات والأرقام التسلسلية — Lot / Serial Traceability (`lot_serial_traceability`)
- fa-route تحليل الاستدعاءات — Recall Analysis (`recall_analysis`)
- fa-route لوحة التجهيز — Staging Board (`staging_board`)

### ops / ops_planning

- fa-chart-gantt · تخطيط الطلب — Demand Planning (`demand_planning`)
- fa-chart-gantt · دقة التنبؤ — Forecast Accuracy (`forecast_accuracy`)
- fa-chart-gantt · تعديلات التنبؤ — Forecast Overrides (`forecast_overrides`)
- fa-chart-gantt · إصدارات التنبؤ — Forecast Versions (`forecast_versions`)
- fa-chart-gantt · جدول الإنتاج الرئيسي — Mps (`mps`)
- fa-chart-gantt · مقترحات الإنتاج — Mps Proposals (`mps_proposals`)
- fa-chart-gantt · استثناءات التخطيط — Planning Exceptions (`planning_exceptions`)
- fa-chart-gantt · مراجعة S&OP — Sop Review (`sop_review`)
- fa-chart-gantt · سيناريوهات S&OP — Sop Scenarios (`sop_scenarios`)
- fa-chart-gantt · توازن العرض والطلب — Supply Demand Balance (`supply_demand_balance`)

### resources / resources_knowledge

- fa-book-open-reader الوثائق — Documents (`documents`)
- fa-book-open-reader التوقيع الإلكتروني — Esign (`esign`)
- fa-book-open-reader قاعدة المعرفة — Knowledge (`knowledge`)
- fa-book-open-reader قاعدة المعرفة (الفنية) — Knowledge Base (`knowledge_base`)
- fa-book-open-reader الاستبيانات والتقييمات — Surveys (`surveys`)
- fa-book-open-reader إدارة الزوّار والاستقبال — Visitors (`visitors`)

### ops / ops_production

- fa-gears لوحة التوقفات — Downtime Board (`downtime_board`)
- fa-gears المكائن — Machines (`machines`)
- fa-gears تخطيط موارد التصنيع — Mrp (`mrp`)
- fa-gears باقات العمليات — Op Packs (`op_packs`)
- fa-gears الأداء التشغيلي — Operational Performance (`operational_performance`)
- fa-gears صرف وإرجاع الإنتاج — Production Issue / Return (`production_issue_return`)
- fa-gears طلبات مواد الإنتاج — Production Material Requests (`production_material_requests`)
- fa-gears استلام الإنتاج — Production Receipt (`production_receipt`)
- fa-gears طابور تعليق الجودة — Quality Hold Queue (`quality_hold_queue`)
- fa-gears مساحة إعادة العمل — Rework Workspace (`rework_workspace`)
- fa-gears اعتماد الهدر — Scrap Approval (`scrap_approval`)
- fa-gears محطة أرضية الإنتاج — Shop-Floor Terminal (`shopfloor_terminal`)
- fa-gears أوامر العمل — Work Orders (`work_orders`)
- fa-gears طابور مركز العمل — Work-Center Queue (`workcenter_queue`)

### ops / ops_kiosks

- fa-tablet-screen-button كشك الموظف — Employee Kiosk (`employee_kiosk`)
- fa-tablet-screen-button روح النظام (كيوسك) — Kiosk (`kiosk`)
- fa-tablet-screen-button سجل أجهزة الأكشاك — Kiosk Device Registry (`kiosk_device_registry`)
- fa-tablet-screen-button كشك الخدمة — Service Kiosk (`service_kiosk`)
- fa-tablet-screen-button كشك أرضية الإنتاج — Shop Floor Kiosk (`shop_floor_kiosk`)
- fa-tablet-screen-button كشك المستودع — Warehouse Kiosk (`warehouse_kiosk`)

### finance / finance_treasury

- fa-money-bill-transfer · تسهيلات التمويل — Financing Facilities (`financing_facilities`)
- fa-money-bill-transfer · تنبؤ السيولة — Liquidity Forecast (`liquidity_forecast`)
- fa-money-bill-transfer · مقترحات الدفع والتمويل — Payment Funding Proposals (`payment_funding_proposals`)
- fa-money-bill-transfer · تنبيهات الخزينة — Treasury Alerts (`treasury_alerts`)
- fa-money-bill-transfer · مركز النقد — Treasury Cash Position (`treasury_cash_position`)

### resources / resources_fleet

- fa-location-dot ربط الأسطول بالأجهزة — Fleet Device Mapping (`fleet_device_mapping`)
- fa-location-dot محاكي الخريطة الحية للأسطول — Fleet Live Map Simulator (`fleet_live_map_simulator`)
- fa-location-dot بيانات الوقود — Fuel Telemetry (`fuel_telemetry`)
- fa-location-dot أحداث السياج الجغرافي — Geofence Events (`geofence_events`)
- fa-location-dot إدارة السياج الجغرافي — Geofence Management (`geofence_management`)
- fa-location-dot محفزات الصيانة — Maintenance Triggers (`maintenance_triggers`)
- fa-location-dot أحداث السرعة والسائق — Speed and Driver Events (`speed_and_driver_events`)
- fa-location-dot طابور فاقد الوقود المشتبه — Suspected Fuel Loss Queue (`suspected_fuel_loss_queue`)
- fa-location-dot الخط الزمني لرحلات المركبة — Vehicle Trip Timeline (`vehicle_trip_timeline`)

### core / core_records

- fa-folder-open دليل الاستخدام — Help Manual (`help_manual`)
- fa-folder-open استيراد البيانات — Import (`import`)
- fa-folder-open إنشاء وصل — Receipt (`receipt`)
- fa-folder-open التقرير النهائي — Report (`report`)

### finance / finance_intercompany

- fa-building-circle-arrow-right · التسوية والمطابقة — Intercompany Reconciliation (`intercompany_reconciliation`)
- fa-building-circle-arrow-right · المعاملات المتبادلة — Intercompany Transactions (`intercompany_transactions`)
- fa-building-circle-arrow-right · قائمة عدم التطابق — Mismatch Queue (`mismatch_queue`)

### commercial / commercial_sales

- fa-cart-shopping برنامج الولاء والمكافآت — Loyalty (`loyalty`)
- fa-cart-shopping نقطة البيع — Pos (`pos`)
- fa-cart-shopping المبيعات والعملاء — Sales (`sales`)
- fa-cart-shopping العمولات — Sales Commission (`sales_commission`)
- fa-cart-shopping قوائم الأسعار — Sales Price Lists (`sales_price_lists`)

### ops / ops_warehouse_inbound

- fa-dolly الاستلام المتنقل — Mobile Receiving (`mobile_receiving`)
- fa-dolly قواعد التخزين — Putaway Rules (`putaway_rules`)
- fa-dolly طابور مهام التخزين — Putaway Task Queue (`putaway_task_queue`)
- fa-dolly فروقات الاستلام — Receiving Discrepancies (`receiving_discrepancies`)
- fa-dolly مقترحات التجديد — Replenishment Proposals (`replenishment_proposals`)
- fa-dolly قواعد التجديد — Replenishment Rules (`replenishment_rules`)
